from django.test import TestCase

# Placeholder tests for the `accounts` app.
# This file restores the original placeholder content but uses a different
# module name to avoid unittest discovery conflicts with the `accounts.tests`
# package used for more granular tests.

class PlaceholderTest(TestCase):
    def test_placeholder(self):
        self.assertTrue(True)
